# Use the official NGINX image as the base image
FROM nginx:latest

# Copy the HTML file into the NGINX container's default HTML directory
COPY index.html /usr/share/nginx/html/

# Expose port 80 (default port for HTTP) to the outside world
EXPOSE 80
